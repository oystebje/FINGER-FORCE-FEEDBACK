%% PRototype code for testing algorithm in arduino sketch for rendering haptic texture

x_min = -0.05; % [m]
x_max = 0.05; % [m]

texture_width = 0.005; %[m]

xh = 0; % Position of handle x in m.
xh_temp = 0;

counter = 0;

for xh = x_min:(texture_width/10):x_max
    
    if xh_temp == texture_width
        counter = counter + texture_width;
    